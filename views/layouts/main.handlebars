<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
<nav>
    <a href="/">Home</a>
    
    <!-- If the user is logged in, allow access to Dashboard, otherwise show an alert -->
    {{#if loggedIn}}
        <a href="/dashboard">Dashboard</a>
    {{else}}
        <a href="#" onclick="alert('Please log in or sign up to access the dashboard!')">Dashboard</a>
    {{/if}}

    {{#if loggedIn}}
        <!-- Show logout button if logged in -->
        <form id="logout-form" action="/users/logout" method="POST" style="display: inline;">
            <button type="submit" id="logout-button">Logout</button>
        </form>
    {{else}}
        <!-- Show login and sign-up options if not logged in -->
        <a href="/login">Login</a>
        <a href="/signup">Sign Up</a>
    {{/if}}
</nav>

    {{{body}}}

    <script src="/js/login.js"></script>
    <script src="/js/signup.js"></script>
    <script src="/js/new-post.js"></script>
    <script src="/js/dashboard.js"></script>
    <script src="/js/comment.js"></script>
</body>
</html>